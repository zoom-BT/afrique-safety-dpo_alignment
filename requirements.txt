# Versions EXACTES de l'image Kaggle, relevees dans le log d'un run reel. Des bornes
# basses ne suffisent pas: les tests construisent les vrais SFTConfig et DPOConfig, donc
# valider contre une autre version que celle qui execute ne prouve rien. Trois runs sont
# morts sur des champs qui existaient ici et pas la-bas.
#
# A reverifier quand Kaggle met a jour son image: le notebook imprime les versions au
# demarrage, c'est la source de verite.
transformers==5.16.1
trl==1.12.0
peft==0.20.0
# Non epingles: fournis par l'image Kaggle avec un CUDA accorde, les figer forcerait une
# reinstallation de plusieurs Go a chaque session.
torch>=2.6
datasets>=4.0
accelerate>=1.0
bitsandbytes>=0.45
wandb
tensorboard
matplotlib
pyyaml
pytest
ruff
